import React from 'react'

interface Props {
    
}

const Settings = (props: Props) => {
    return (
        <div>
            SETTINGS
        </div>
    )
}

export default Settings
